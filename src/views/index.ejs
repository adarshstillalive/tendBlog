<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TendBlog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <style>
    .card-text {
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
  </style>
</head>
<body>
  <nav class="navbar bg-secondary p-3">
    <div class="container-fluid">
      <a class="navbar-brand text-light">tendBlog.</a>
    </div>
  </nav>

  <a href="addBlog" class="btn btn-primary m-5">Add blog</a>

  <div class="container mt-5">
    <div class="row justify-content-center">
      <% for(let i = 0; i < contents.length; i++) { %>
        <div class="col-md-6">
          <div class="card mb-4" style="height: 14rem;">
            <div class="card-body">
              <a href="/viewBlog/<%= contents[i].id %>" class="text-decoration-none">
                <h5 class="card-title text-dark"><%= contents[i].title %></h5>
              </a>
              <h6 class="card-subtitle mb-2 text-muted">
                By: <%= contents[i].authorId %>
              </h6>
              <p class="card-text">
                <%= contents[i].description %>...
              </p>
              <% if(user.id===contents[i].authorId){ %>
              <a href="/editContent/<%= contents[i].id %>" class="card-link">Edit post</a>
              <%}%>
              <span class="text-muted float-end">
                <%= new Date(contents[i].createdAt).toLocaleDateString() %>
              </span>
            </div>
            <% if(user.id===contents[i].authorId){ %>
              <a href="/deleteBlog/<%= contents[i].id %>" class="btn btn-danger m-3">Delete post</a>
              <%}%>
          </div>
        </div>
      <% } %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
