<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content.title %> - TendBlog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .content-container {
      max-width: 800px;
      margin: 0 auto;
      padding-top: 50px;
    }
  </style>
</head>
<body>
  <nav class="navbar bg-secondary p-3">
    <div class="container-fluid">
      <a class="navbar-brand text-light">tendBlog.</a>
    </div>
  </nav>

  <div class="container content-container">
    <div class="card shadow-sm">
      <div class="card-body">
        <h1 class="card-title mb-4"><%= content.title %></h1>
        <h6 class="text-muted">By Author ID: <%= content.authorId %> | Created At: <%= content.createdAt.toLocaleDateString() %></h6>
        <p class="card-text mt-4"><%= content.description %></p>

        <% if (content.reference) { %>
          <p><strong>Reference:</strong> <a href="<%= content.reference %>"><%= content.reference %></a></p>
        <% } %>

        <div class="mt-4">
          <a href="/" class="btn btn-primary">Back to Home</a>
          <% if(user.id === content.authorId) { %>
            <a href="/editContent/<%= content.id %>" class="btn btn-secondary">Edit Post</a>
          <% } %>
        </div>
      </div>
      <% if(user.id===content.authorId){%>
      <a href="/deleteBlog/<%= content.id %>" class="btn btn-danger m-3">Delete</a>
      <%}%>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
